{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/db run push || echo 'WARNING: Database push failed. Ensure DATABASE_URL is configured correctly.' && pnpm --filter @workspace/anvikshiki build",
  "outputDirectory": "artifacts/anvikshiki/dist/public",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
