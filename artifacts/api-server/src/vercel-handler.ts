// Vercel serverless handler entry point.
// This file is the esbuild entry for the pre-built Vercel bundle.
// It exports the Express app as the default export for Vercel's serverless adapter.
import app from "./app";

export default app;
