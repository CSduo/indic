Fix the homepage hero implementation. The current result is wrong because the new design image was added like a static picture below the old header, and the old header is still visible. I do not want that.

Use the uploaded reference image only as the visual design reference. Do not paste it as one flat screenshot inside the page. Rebuild the hero as real HTML/CSS/React components.

Remove or replace the old top dark header completely on the homepage. There should not be two headers. The homepage should start with one clean functional header based on the new design.

Create a real clickable website header with:

Logo/emblem on the left
Clickable website name “ĀNVĪKṢIKĪ”
Small subtitle “JOURNAL & RESEARCH PLATFORM”
Search icon/button on the right
Menu icon/button on the right

The website name/logo must be clickable and should take the user to the homepage `/`.

The search icon must be clickable. On click, it should open the existing search page, search overlay, or route `/search`. If a search overlay already exists, connect it properly. Do not leave the search icon as decoration.

The menu icon must open the existing menu/navigation drawer. If the menu drawer already exists, connect it. If it does not exist, create a simple mobile navigation drawer with Home, Explore, Domains, Papers, Archive, Submit, Community, About, Account/Admin where appropriate.

The uploaded hero artwork should not be cropped incorrectly. It should be used as the main hero illustration area, but the layout must be responsive.

Important image handling:

Do not use the whole uploaded screenshot as a fake UI image.
If using the image as artwork, use only the illustration/hero art portion if possible, not the fake header inside the image.
If the full generated design image is used, display it with `width: 100%`, `height: auto`, `object-fit: contain`, and no forced cropping.
Do not use `object-fit: cover` if it cuts off important parts.
On mobile, the full hero composition should be visible.
On desktop, center it inside a beautiful responsive frame and do not stretch it badly.

The new homepage hero should be built like this:

1. Real functional header at the top.
2. Hero visual/illustration section below it.
3. Real title text below or over the hero: “ĀNVĪKṢIKĪ”.
4. Real subtitle text: “A Journal of Inquiry & Civilizational Thought”.
5. Real poetic line: “A journey through archives and ideas, where inquiry becomes insight and first essays find their home.”
6. Real clickable buttons:
   Submit Your Work → `/submit`
   Explore Journal → `/explore` or `/domains`
   Join Community → `/community` or newsletter/community section

Do not put these buttons as part of a flat image. They must be actual clickable buttons.

Make sure the design visually matches the uploaded reference:
2D illustrated journey style
Parchment background
Muted earthy colors
Hand-drawn linework feel
Scholar/traveler journey atmosphere
Calm civilizational archive-garden mood
Elegant serif typography
Simple but premium UI

Also fix the duplicate-header issue. There must be only one active header.

Technical requirements:

If this is a React/Next app, create or update a proper `HeroSection` component.
Create or update a `Header` component.
Make the header sticky only if it looks good; otherwise keep it normal.
Ensure the header works on mobile and desktop.
Ensure logo/title click navigates to `/`.
Ensure search click navigates to `/search` or opens the search overlay.
Ensure menu click opens the mobile drawer.
Ensure CTA buttons navigate correctly.
Ensure the page starts at top when navigating.
Ensure the image is not cropped unintentionally.
Remove old unused header markup/CSS if it conflicts.
Remove any duplicate hero image rendering.
Make the homepage feel like one coherent designed section, not an image pasted into an old website.

After fixing, verify:

The old header is gone or replaced.
Only one header is visible.
The logo/name is clickable.
Search icon works.
Menu icon works.
Submit button works.
Explore button works.
Join Community button works.
The full hero image/art is visible and not badly cropped.
The page looks good on mobile and desktop.
